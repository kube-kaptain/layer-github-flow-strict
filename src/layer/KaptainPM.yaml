apiVersion: kaptain.org/1.7
metadata:
  description: "GitHub flow strict quality enforcement layer"

spec:
  main:
    quality:
      branches:
        blockSlashes: true
      commits:
        blockConventional: true
